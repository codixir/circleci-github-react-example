orbs:
  react: "thefrontside/react@dev:alpha"
version: 2.1 #version
workflows:
  push:
    jobs:
      - react/install
      - react/eslint:
          requires:
            - react/install
      - react/test
          requires
            - requires/install
      - react/coverage:
          requires:
            - react/install
